# 智能校园失物招领平台 - 后端服务

> 💡 **Windows 用户快速开始**：查看 [Windows 环境设置指南](docs/WINDOWS_SETUP.md)

## 项目状态

✅ **已完成模块**

### 第一阶段：项目初始化与基础架构
- Spring Boot 项目初始化与依赖配置
- 数据库表结构设计与创建（7张核心表）
- MyBatis-Plus 基础代码生成（Entity + Mapper）
- 分页插件与自动填充时间字段配置
- 统一响应格式与全局异常处理

### 第二阶段：用户认证模块 ⭐
- 用户注册功能（学号/手机号唯一性验证）
- 用户登录功能（密码验证、JWT令牌生成）
- JWT令牌认证（访问令牌2小时 + 刷新令牌7天）
- 登录失败5次锁定机制（15分钟自动解锁）
- 每日首次登录积分奖励（+2积分）
- 用户信息管理（查询、更新、头像上传）

### 第三阶段：文件上传模块 ⭐
- 阿里云OSS集成配置
- 图片上传（支持jpg/jpeg/png/gif/webp）
- 文件大小限制（单文件≤10MB）
- 大图自动压缩（>5MB自动压缩）
- 批量上传支持（最多9张）

### 第四阶段：物品信息模块 ⭐
- 物品发布（失物/招领）
- 物品编辑与软删除
- 物品详情查看（含浏览计数）
- 发布招领信息积分奖励（+10积分）

### 第五阶段：AI图像识别模块 ⭐
- 阿里云视觉智能SDK集成
- 图片自动识别物品类别
- 特征标签提取（颜色、品牌、型号等）
- AI识别失败降级处理
- 手动类别选择接口

### 第六阶段：LBS地理服务模块 ⭐
- 高德地图API集成
- 地址转坐标（地理编码）
- 坐标转地址（逆地理编码）
- 两点距离计算（Haversine公式）
- 附近物品范围查询

### 第七阶段：搜索与筛选模块 ⭐
- 关键词全文搜索（标题/描述/标签）
- 多条件筛选（类型/类别/状态/时间范围）
- 地理范围筛选
- 多种排序方式（时间/距离）
- 分页查询
- 热门物品查询

### 第八阶段：智能匹配模块 ⭐
- 匹配分数计算算法
  - 类别匹配（30%权重）
  - 标签相似度（30%权重，Jaccard算法）
  - 时间接近度（20%权重）
  - 地理距离（20%权重）
- 匹配推荐列表（Top 10）
- 匹配确认功能
- 匹配反馈收集
- 自动匹配通知（分数≥70自动推送）

### 第九阶段：消息通知模块 ⭐
- 站内消息发送与接收
- 消息类型分类（系统/匹配/留言）
- 消息列表查询与筛选
- 已读/未读状态管理
- WebSocket实时推送

### 第十阶段：积分模块 ⭐
- 积分奖励机制
  - 每日登录 +2积分
  - 发布招领 +10积分
  - 帮助找回 +50积分
- 积分明细查询
- 积分排行榜（Top 100）

### 第十一阶段：管理后台模块 ⭐
- 数据统计仪表盘
- 内容管理（物品审核）
- 用户管理（封禁/解封）
- 数据导出（Excel报表）

### 第十二阶段：系统优化 ⭐
- Redis缓存优化
  - 物品详情缓存（30分钟）
  - 用户信息缓存（1小时）
  - 搜索结果缓存（5分钟）
  - 统计数据缓存（5分钟）
  - 积分排行榜缓存（ZSET）
- 缓存更新策略

📋 **待开发**
- 接口限流（100请求/分钟/IP）
- Swagger/OpenAPI文档
- 前端Vue 3项目

## 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | Spring Boot 3.4.1 |
| Java版本 | Java 21 |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7.x |
| ORM | MyBatis-Plus 3.5.5 |
| 安全 | Spring Security + JWT |
| 文件存储 | 阿里云OSS |
| AI服务 | 阿里云视觉智能（图像识别） |
| 地图服务 | 高德地图API |
| 测试框架 | JUnit 5 + jqwik（属性测试） |

## 项目结构

```
lostandfound/
├── docs/                            # 项目文档
│   ├── database-schema.md          # 数据库设计文档
│   ├── database-setup-guide.md     # 数据库设置指南
│   ├── oss-integration-guide.md    # OSS集成指南
│   └── WINDOWS_SETUP.md            # Windows环境设置
├── scripts/                         # 脚本文件
│   ├── init-database.bat           # 数据库初始化（Windows）
│   └── verify-database.sql         # 数据库验证SQL
├── src/
│   ├── main/java/com/campus/lostandfound/
│   │   ├── config/                  # 配置类
│   │   │   ├── SecurityConfig.java         # Spring Security配置
│   │   │   ├── JwtAuthenticationFilter.java # JWT认证过滤器
│   │   │   ├── MyBatisPlusConfig.java      # MyBatis-Plus配置
│   │   │   ├── OssConfig.java              # 阿里云OSS配置
│   │   │   ├── AliyunVisionConfig.java     # 阿里云视觉智能配置
│   │   │   ├── AmapConfig.java             # 高德地图配置
│   │   │   ├── RedisCacheConfig.java       # Redis缓存配置
│   │   │   ├── AsyncConfig.java            # 异步任务配置
│   │   │   └── WebSocketConfig.java        # WebSocket配置
│   │   ├── controller/              # REST API控制器
│   │   │   ├── AuthController.java         # 认证接口
│   │   │   ├── UserController.java         # 用户接口
│   │   │   ├── FileController.java         # 文件上传接口
│   │   │   ├── ItemController.java         # 物品信息接口
│   │   │   ├── MatchController.java        # 匹配接口
│   │   │   ├── MessageController.java      # 消息接口
│   │   │   ├── PointController.java        # 积分接口
│   │   │   └── AdminController.java        # 管理后台接口
│   │   ├── service/                 # 服务层
│   │   │   ├── AuthService.java            # 认证服务
│   │   │   ├── UserService.java            # 用户服务
│   │   │   ├── FileService.java            # 文件服务
│   │   │   ├── ItemService.java            # 物品服务
│   │   │   ├── MatchService.java           # 匹配服务
│   │   │   ├── MessageService.java         # 消息服务
│   │   │   ├── PointService.java           # 积分服务
│   │   │   ├── AdminService.java           # 管理服务
│   │   │   ├── ImageRecognitionService.java # 图像识别服务
│   │   │   ├── LocationService.java        # 地理位置服务
│   │   │   ├── CacheService.java           # 缓存服务
│   │   │   └── impl/                       # 服务实现
│   │   ├── repository/              # 数据访问层（Mapper）
│   │   ├── model/                   # 数据模型
│   │   │   ├── entity/              # 实体类
│   │   │   ├── dto/                 # 数据传输对象
│   │   │   ├── vo/                  # 视图对象
│   │   │   └── enums/               # 枚举类
│   │   ├── common/                  # 公共组件
│   │   ├── exception/               # 异常处理
│   │   ├── util/                    # 工具类
│   │   └── websocket/               # WebSocket处理器
│   └── main/resources/
│       ├── application.yml          # 主配置文件
│       ├── application-local.yml    # 本地开发配置
│       └── schema.sql               # 数据库建表脚本
└── src/test/                        # 测试代码
```

## 数据模型

| 实体类 | 数据库表 | 说明 |
|--------|---------|------|
| User | user | 用户信息 |
| Item | item | 物品信息（失物/招领）|
| ItemImage | item_image | 物品图片 |
| ItemTag | item_tag | 物品标签（AI识别） |
| MatchRecord | match_record | 匹配记录 |
| MatchFeedback | match_feedback | 匹配反馈 |
| Message | message | 消息通知 |
| PointRecord | point_record | 积分记录 |

## 快速开始

### 1. 环境要求

- JDK 21+
- MySQL 8.0+
- Redis 7.x+
- Maven 3.8+

### 2. 配置本地环境

复制并修改 `application-local.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campuslostandfound?serverTimezone=Asia/Shanghai
    username: your_username
    password: your_password
  redis:
    host: localhost
    port: 6379
    password: your_redis_password  # 如果设置了密码

jwt:
  secret: your-256-bit-secret-key-here-at-least-32-characters-long

# 阿里云OSS配置
aliyun:
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    access-key-id: your_access_key_id
    access-key-secret: your_access_key_secret
    bucket-name: your_bucket_name
  vision:
    region-id: cn-shanghai

# 高德地图配置
amap:
  key: your_amap_key
```

### 3. 初始化数据库

```bash
mysql -u root -p < src/main/resources/schema.sql
```

### 4. 启动Redis

```bash
redis-server
```

### 5. 编译运行

```bash
# Windows
.\mvnw.cmd clean compile
.\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local

# Linux/Mac
./mvnw clean compile
./mvnw spring-boot:run -Dspring-boot.run.profiles=local
```

### 6. 运行测试

```bash
.\mvnw.cmd clean verify
```

## API接口

### 认证接口 `/api/v1/auth`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /register | 用户注册 |
| POST | /login | 用户登录 |
| POST | /refresh | 刷新令牌 |

### 用户接口 `/api/v1/users`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /me | 获取当前用户信息 | 需要 |
| PUT | /me | 更新当前用户信息 | 需要 |
| GET | / | 查询用户列表（管理员） | 需要 |

### 文件接口 `/api/v1/files`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /upload | 上传单个文件 | 需要 |
| POST | /upload/batch | 批量上传（最多9张） | 需要 |

### 物品接口 `/api/v1/items`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | / | 发布物品信息 | 需要 |
| GET | / | 搜索物品列表 | 否 |
| GET | /{id} | 获取物品详情 | 否 |
| PUT | /{id} | 更新物品信息 | 需要 |
| DELETE | /{id} | 删除物品信息 | 需要 |
| PUT | /{id}/category | 手动更新类别 | 需要 |
| GET | /nearby | 附近物品搜索 | 否 |
| GET | /hot | 热门物品 | 否 |
| GET | /my | 我的发布 | 需要 |

### 匹配接口 `/api/v1/matches`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /recommendations/{itemId} | 获取匹配推荐 | 需要 |
| POST | /confirm | 确认匹配 | 需要 |
| POST | /feedback | 提交匹配反馈 | 需要 |

### 消息接口 `/api/v1/messages`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取消息列表 | 需要 |
| GET | /unread-count | 获取未读数量 | 需要 |
| PUT | /{id}/read | 标记已读 | 需要 |
| PUT | /read-all | 全部标记已读 | 需要 |

### 积分接口 `/api/v1/points`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取积分明细 | 需要 |
| GET | /total | 获取总积分 | 需要 |
| GET | /ranking | 积分排行榜 | 否 |

### 管理接口 `/api/v1/admin`（需要管理员权限）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /statistics | 获取统计数据 |
| GET | /items | 管理员物品列表 |
| POST | /items/{id}/review | 审核物品 |
| POST | /users/{id}/ban | 封禁用户 |
| POST | /users/{id}/unban | 解封用户 |
| GET | /export | 导出数据报表 |

## 环境变量

生产环境建议使用环境变量配置敏感信息：

| 变量名 | 说明 |
|--------|------|
| DB_USERNAME | 数据库用户名 |
| DB_PASSWORD | 数据库密码 |
| REDIS_HOST | Redis主机地址 |
| REDIS_PASSWORD | Redis密码 |
| JWT_SECRET | JWT密钥（至少32字符） |
| OSS_ENDPOINT | 阿里云OSS端点 |
| OSS_ACCESS_KEY_ID | OSS访问密钥ID |
| OSS_ACCESS_KEY_SECRET | OSS访问密钥 |
| OSS_BUCKET_NAME | OSS存储桶名称 |
| ALIYUN_VISION_REGION_ID | 阿里云视觉智能区域ID |
| AMAP_KEY | 高德地图API密钥 |

## 相关文档

- [数据库设计文档](docs/database-schema.md)
- [数据库设置指南](docs/database-setup-guide.md)
- [Windows环境设置](docs/WINDOWS_SETUP.md)
- [OSS集成指南](docs/oss-integration-guide.md)
- [认证接口测试指南](test-auth-endpoints.md)

## 常见问题

### 编译错误
使用 Maven Wrapper：`.\mvnw.cmd clean compile`

### 数据库连接失败
1. 确认MySQL服务已启动
2. 检查 `application-local.yml` 配置
3. 确认数据库 `campuslostandfound` 已创建

### Redis连接失败
1. 确认Redis服务已启动
2. 测试连接：`redis-cli ping`

### JWT令牌验证失败
1. 确认请求头包含 `Authorization: Bearer <token>`
2. 检查令牌是否过期
3. 使用刷新令牌获取新的访问令牌

### 登录账户锁定
等待15分钟自动解锁，或手动清除Redis：`DEL login:fail:{studentId}`

## 许可证

[待定]
