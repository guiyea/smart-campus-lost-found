# 智能校园失物招领平台

<div align="center">

**基于 AI 图像识别和智能匹配的校园失物招领系统**

[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.4.1-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![Java](https://img.shields.io/badge/Java-21-orange.svg)](https://www.oracle.com/java/)
[![Vue](https://img.shields.io/badge/Vue-3.x-green.svg)](https://vuejs.org/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

[快速开始](#快速开始) • [功能特性](#功能特性) • [技术架构](#技术架构) • [文档](#文档) • [部署](#部署)

</div>

---

## 📖 项目简介

智能校园失物招领平台是一个现代化的失物招领管理系统，集成了 AI 图像识别、智能匹配算法、LBS 地理服务等先进技术，为校园师生提供高效便捷的失物招领服务。

### 核心亮点

- 🤖 **AI 图像识别**：自动识别物品类别和特征标签
- 🎯 **智能匹配算法**：基于多维度相似度的智能推荐
- 📍 **LBS 地理服务**：支持地理位置查询和附近物品搜索
- 💬 **实时消息通知**：WebSocket 实时推送匹配通知
- 🏆 **积分激励系统**：鼓励用户积极参与失物招领
- 📊 **数据统计分析**：完善的管理后台和数据报表

## 项目状态

### 开发进度

| 模块 | 状态 | 完成度 |
|------|------|--------|
| 基础架构 | ✅ 完成 | 100% |
| 用户认证 | ✅ 完成 | 100% |
| 文件上传 | ✅ 完成 | 100% |
| 物品管理 | ✅ 完成 | 100% |
| AI 识别 | ✅ 完成 | 100% |
| LBS 服务 | ✅ 完成 | 100% |
| 搜索筛选 | ✅ 完成 | 100% |
| 智能匹配 | ✅ 完成 | 100% |
| 消息通知 | ✅ 完成 | 100% |
| 积分系统 | ✅ 完成 | 100% |
| 管理后台 | ✅ 完成 | 100% |
| 系统优化 | ✅ 完成 | 100% |
| 前端界面 | ✅ 完成 | 100% |
| 部署配置 | ✅ 完成 | 100% |

## 功能特性

### 🔐 用户认证与授权
- 用户注册/登录（学号/手机号）
- JWT 令牌认证（访问令牌 2h + 刷新令牌 7d）
- 登录失败 5 次锁定机制（15 分钟自动解锁）
- 角色权限管理（普通用户/管理员）

### 📦 物品管理
- 发布失物/招领信息
- 物品编辑与软删除
- 多图上传（最多 9 张）
- 物品详情查看（含浏览计数）
- 物品状态管理（待认领/已认领/已关闭）

### 🤖 AI 智能识别
- 自动识别物品类别（基于阿里云视觉智能）
- 提取特征标签（颜色、品牌、型号等）
- AI 识别失败降级处理
- 支持手动修正类别

### 🎯 智能匹配算法
- 多维度相似度计算
  - 类别匹配（30% 权重）
  - 标签相似度（30% 权重，Jaccard 算法）
  - 时间接近度（20% 权重）
  - 地理距离（20% 权重）
- Top 10 匹配推荐
- 匹配确认与反馈
- 自动匹配通知（分数 ≥70）

### 📍 LBS 地理服务
- 地址转坐标（地理编码）
- 坐标转地址（逆地理编码）
- 两点距离计算（Haversine 公式）
- 附近物品范围查询

### 🔍 搜索与筛选
- 关键词全文搜索
- 多条件筛选（类型/类别/状态/时间）
- 地理范围筛选
- 多种排序方式（时间/距离/热度）
- 分页查询

### 💬 消息通知
- 站内消息系统
- 消息类型分类（系统/匹配/留言）
- WebSocket 实时推送
- 已读/未读状态管理

### 🏆 积分激励
- 每日登录 +2 积分
- 发布招领 +10 积分
- 帮助找回 +50 积分
- 积分明细查询
- 积分排行榜（Top 100）

### 📊 管理后台
- 数据统计仪表盘
- 物品审核管理
- 用户管理（封禁/解封）
- 数据导出（Excel 报表）

### ⚡ 性能优化
- Redis 多级缓存
- 数据库索引优化
- 异步任务处理
- 接口限流保护

## 技术架构

### 后端技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心框架 | Spring Boot | 3.4.1 | 应用框架 |
| 语言 | Java | 21 | 编程语言 |
| 数据库 | MySQL | 8.0+ | 关系型数据库 |
| 缓存 | Redis | 7.x+ | 缓存和消息队列 |
| ORM | MyBatis-Plus | 3.5.5 | 数据持久化 |
| 安全 | Spring Security + JWT | - | 认证授权 |
| 文件存储 | 阿里云 OSS | - | 对象存储 |
| AI 服务 | 阿里云视觉智能 | - | 图像识别 |
| 地图服务 | 高德地图 API | - | LBS 服务 |
| 测试框架 | JUnit 5 + jqwik | - | 单元测试 + 属性测试 |
| 文档 | Swagger/OpenAPI | 3.0 | API 文档 |

### 前端技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心框架 | Vue | 3.x | 渐进式框架 |
| 构建工具 | Vite | 5.x | 前端构建工具 |
| 语言 | TypeScript | 5.x | 类型安全 |
| UI 框架 | Element Plus | 2.x | 组件库 |
| 状态管理 | Pinia | 2.x | 状态管理 |
| 路由 | Vue Router | 4.x | 路由管理 |
| HTTP 客户端 | Axios | 1.x | HTTP 请求 |
| 地图组件 | 高德地图 JS API | 2.0 | 地图展示 |

### 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      前端层 (Vue 3)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐│
│  │ 用户界面 │  │ 管理后台 │  │ 地图组件 │  │ 消息推送 ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘│
└─────────────────────────────────────────────────────────┘
                          ↓ HTTP/WebSocket
┌─────────────────────────────────────────────────────────┐
│                   API 网关 / Nginx                       │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                 应用层 (Spring Boot)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐│
│  │ 认证服务 │  │ 物品服务 │  │ 匹配服务 │  │ 消息服务 ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘│
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐│
│  │ 文件服务 │  │ 积分服务 │  │ 管理服务 │  │ 缓存服务 ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘│
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│                    数据层 & 外部服务                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐│
│  │  MySQL   │  │  Redis   │  │ 阿里云OSS│  │ 阿里云AI ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘│
│  ┌──────────┐                                            │
│  │ 高德地图 │                                            │
│  └──────────┘                                            │
└─────────────────────────────────────────────────────────┘
```

## 项目结构

```
lostandfound/
├── docs/                            # 项目文档
│   ├── database-schema.md          # 数据库设计文档
│   ├── database-setup-guide.md     # 数据库设置指南
│   ├── oss-integration-guide.md    # OSS集成指南
│   └── WINDOWS_SETUP.md            # Windows环境设置
├── scripts/                         # 脚本文件
│   ├── init-database.bat           # 数据库初始化（Windows）
│   └── verify-database.sql         # 数据库验证SQL
├── src/
│   ├── main/java/com/campus/lostandfound/
│   │   ├── config/                  # 配置类
│   │   │   ├── SecurityConfig.java         # Spring Security配置
│   │   │   ├── JwtAuthenticationFilter.java # JWT认证过滤器
│   │   │   ├── MyBatisPlusConfig.java      # MyBatis-Plus配置
│   │   │   ├── OssConfig.java              # 阿里云OSS配置
│   │   │   ├── AliyunVisionConfig.java     # 阿里云视觉智能配置
│   │   │   ├── AmapConfig.java             # 高德地图配置
│   │   │   ├── RedisCacheConfig.java       # Redis缓存配置
│   │   │   ├── AsyncConfig.java            # 异步任务配置
│   │   │   └── WebSocketConfig.java        # WebSocket配置
│   │   ├── controller/              # REST API控制器
│   │   │   ├── AuthController.java         # 认证接口
│   │   │   ├── UserController.java         # 用户接口
│   │   │   ├── FileController.java         # 文件上传接口
│   │   │   ├── ItemController.java         # 物品信息接口
│   │   │   ├── MatchController.java        # 匹配接口
│   │   │   ├── MessageController.java      # 消息接口
│   │   │   ├── PointController.java        # 积分接口
│   │   │   └── AdminController.java        # 管理后台接口
│   │   ├── service/                 # 服务层
│   │   │   ├── AuthService.java            # 认证服务
│   │   │   ├── UserService.java            # 用户服务
│   │   │   ├── FileService.java            # 文件服务
│   │   │   ├── ItemService.java            # 物品服务
│   │   │   ├── MatchService.java           # 匹配服务
│   │   │   ├── MessageService.java         # 消息服务
│   │   │   ├── PointService.java           # 积分服务
│   │   │   ├── AdminService.java           # 管理服务
│   │   │   ├── ImageRecognitionService.java # 图像识别服务
│   │   │   ├── LocationService.java        # 地理位置服务
│   │   │   ├── CacheService.java           # 缓存服务
│   │   │   └── impl/                       # 服务实现
│   │   ├── repository/              # 数据访问层（Mapper）
│   │   ├── model/                   # 数据模型
│   │   │   ├── entity/              # 实体类
│   │   │   ├── dto/                 # 数据传输对象
│   │   │   ├── vo/                  # 视图对象
│   │   │   └── enums/               # 枚举类
│   │   ├── common/                  # 公共组件
│   │   ├── exception/               # 异常处理
│   │   ├── util/                    # 工具类
│   │   └── websocket/               # WebSocket处理器
│   └── main/resources/
│       ├── application.yml          # 主配置文件
│       ├── application-local.yml    # 本地开发配置
│       └── schema.sql               # 数据库建表脚本
└── src/test/                        # 测试代码
```

## 数据模型

| 实体类 | 数据库表 | 说明 |
|--------|---------|------|
| User | user | 用户信息 |
| Item | item | 物品信息（失物/招领）|
| ItemImage | item_image | 物品图片 |
| ItemTag | item_tag | 物品标签（AI识别） |
| MatchRecord | match_record | 匹配记录 |
| MatchFeedback | match_feedback | 匹配反馈 |
| Message | message | 消息通知 |
| PointRecord | point_record | 积分记录 |

## 快速开始

> 💡 **Windows 用户**：查看 [Windows 环境设置指南](docs/WINDOWS_SETUP.md)  
> 🚀 **快速启动**：查看 [快速开始指南](快速开始.md)

### 环境要求

- **JDK**: 21+
- **Maven**: 3.8+ (或使用项目自带的 Maven Wrapper)
- **MySQL**: 8.0+
- **Redis**: 7.x+
- **Node.js**: 18+ (前端开发)

### 1. 克隆项目

```bash
git clone <repository-url>
cd smart-campus-lost-found
```

### 2. 配置数据库

```bash
# 创建数据库
mysql -u root -p
CREATE DATABASE campuslostandfound CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入表结构
mysql -u root -p campuslostandfound < lostandfound/src/main/resources/schema.sql

# (可选) 导入测试数据
mysql -u root -p campuslostandfound < lostandfound/src/main/resources/data-test.sql
```

**Windows 用户可使用脚本**：
```cmd
cd lostandfound\scripts
init-database.bat
```

### 3. 配置应用

复制并修改配置文件：

```bash
cd lostandfound/src/main/resources
cp application-local.yml.example application-local.yml
```

编辑 `application-local.yml`，配置以下信息：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campuslostandfound?serverTimezone=Asia/Shanghai
    username: your_username
    password: your_password
  redis:
    host: localhost
    port: 6379
    password: your_redis_password  # 如果设置了密码

jwt:
  secret: your-256-bit-secret-key-here-at-least-32-characters-long

# 阿里云 OSS 配置
aliyun:
  oss:
    endpoint: oss-cn-hangzhou.aliyuncs.com
    access-key-id: ${OSS_ACCESS_KEY_ID}
    access-key-secret: ${OSS_ACCESS_KEY_SECRET}
    bucket-name: your_bucket_name
  vision:
    region-id: cn-shanghai
    access-key-id: ${ALIYUN_ACCESS_KEY_ID}
    access-key-secret: ${ALIYUN_ACCESS_KEY_SECRET}

# 高德地图配置
amap:
  key: ${AMAP_KEY}
```

### 4. 启动后端服务

```bash
cd lostandfound

# Windows
.\mvnw.cmd clean compile
.\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local

# Linux/Mac
./mvnw clean compile
./mvnw spring-boot:run -Dspring-boot.run.profiles=local
```

后端服务将在 `http://localhost:8080` 启动

### 5. 启动前端服务

```bash
cd web

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端服务将在 `http://localhost:5173` 启动

### 6. 访问应用

- **前端界面**: http://localhost:5173
- **后端 API**: http://localhost:8080/api/v1
- **API 文档**: http://localhost:8080/swagger-ui.html

### 7. 运行测试

```bash
cd lostandfound

# 运行所有测试
.\mvnw.cmd clean verify

# 运行特定测试
.\mvnw.cmd test -Dtest=AuthServiceTest
```

## API接口

### 认证接口 `/api/v1/auth`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /register | 用户注册 |
| POST | /login | 用户登录 |
| POST | /refresh | 刷新令牌 |

### 用户接口 `/api/v1/users`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /me | 获取当前用户信息 | 需要 |
| PUT | /me | 更新当前用户信息 | 需要 |
| GET | / | 查询用户列表（管理员） | 需要 |

### 文件接口 `/api/v1/files`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /upload | 上传单个文件 | 需要 |
| POST | /upload/batch | 批量上传（最多9张） | 需要 |

### 物品接口 `/api/v1/items`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | / | 发布物品信息 | 需要 |
| GET | / | 搜索物品列表 | 否 |
| GET | /{id} | 获取物品详情 | 否 |
| PUT | /{id} | 更新物品信息 | 需要 |
| DELETE | /{id} | 删除物品信息 | 需要 |
| PUT | /{id}/category | 手动更新类别 | 需要 |
| GET | /nearby | 附近物品搜索 | 否 |
| GET | /hot | 热门物品 | 否 |
| GET | /my | 我的发布 | 需要 |

### 匹配接口 `/api/v1/matches`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /recommendations/{itemId} | 获取匹配推荐 | 需要 |
| POST | /confirm | 确认匹配 | 需要 |
| POST | /feedback | 提交匹配反馈 | 需要 |

### 消息接口 `/api/v1/messages`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取消息列表 | 需要 |
| GET | /unread-count | 获取未读数量 | 需要 |
| PUT | /{id}/read | 标记已读 | 需要 |
| PUT | /read-all | 全部标记已读 | 需要 |

### 积分接口 `/api/v1/points`

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | / | 获取积分明细 | 需要 |
| GET | /total | 获取总积分 | 需要 |
| GET | /ranking | 积分排行榜 | 否 |

### 管理接口 `/api/v1/admin`（需要管理员权限）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /statistics | 获取统计数据 |
| GET | /items | 管理员物品列表 |
| POST | /items/{id}/review | 审核物品 |
| POST | /users/{id}/ban | 封禁用户 |
| POST | /users/{id}/unban | 解封用户 |
| GET | /export | 导出数据报表 |

## 环境变量

生产环境建议使用环境变量配置敏感信息：

| 变量名 | 说明 |
|--------|------|
| DB_USERNAME | 数据库用户名 |
| DB_PASSWORD | 数据库密码 |
| REDIS_HOST | Redis主机地址 |
| REDIS_PASSWORD | Redis密码 |
| JWT_SECRET | JWT密钥（至少32字符） |
| OSS_ENDPOINT | 阿里云OSS端点 |
| OSS_ACCESS_KEY_ID | OSS访问密钥ID |
| OSS_ACCESS_KEY_SECRET | OSS访问密钥 |
| OSS_BUCKET_NAME | OSS存储桶名称 |
| ALIYUN_VISION_REGION_ID | 阿里云视觉智能区域ID |
| AMAP_KEY | 高德地图API密钥 |

## 文档

### 📚 开发文档

- [项目需求文档](../.kiro/specs/smart-campus-lost-found/requirements.md)
- [系统设计文档](../.kiro/specs/smart-campus-lost-found/design.md)
- [任务清单](../.kiro/specs/smart-campus-lost-found/tasks.md)
- [数据库设计文档](docs/database-schema.md)
- [API 接口文档](#api接口)

### 🔧 配置指南

- [Windows 环境设置](docs/WINDOWS_SETUP.md)
- [数据库设置指南](docs/database-setup-guide.md)
- [快速开始指南](快速开始.md)
- [OSS 集成指南](docs/oss-integration-guide.md)

### 🧪 测试文档

- [认证接口测试](test-auth-endpoints.md)
- [文件上传测试](test-file-upload.md)
- [Checkpoint 8 验证报告](checkpoint-8-verification-report.md)

### 📊 毕业设计相关

- [毕业设计开题报告](docs/graduation-proposal.md)
- [毕业设计调查问卷](docs/graduation-survey.md)
- [问卷数据分析指南](docs/survey-analysis-guide.md)
- [问卷数据模板指南](docs/survey-data-template-guide.md)

## 部署

### Docker 部署

项目提供了完整的 Docker 部署方案：

```bash
cd lostandfound/deploy

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入实际配置

# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

详细部署说明请查看 [部署文档](deploy/DEPLOYMENT.md)

### 生产环境配置

生产环境建议使用环境变量配置敏感信息：

| 变量名 | 说明 | 必需 |
|--------|------|------|
| `DB_USERNAME` | 数据库用户名 | ✅ |
| `DB_PASSWORD` | 数据库密码 | ✅ |
| `REDIS_HOST` | Redis 主机地址 | ✅ |
| `REDIS_PASSWORD` | Redis 密码 | ❌ |
| `JWT_SECRET` | JWT 密钥（至少 32 字符） | ✅ |
| `OSS_ENDPOINT` | 阿里云 OSS 端点 | ✅ |
| `OSS_ACCESS_KEY_ID` | OSS 访问密钥 ID | ✅ |
| `OSS_ACCESS_KEY_SECRET` | OSS 访问密钥 | ✅ |
| `OSS_BUCKET_NAME` | OSS 存储桶名称 | ✅ |
| `ALIYUN_VISION_REGION_ID` | 阿里云视觉智能区域 ID | ✅ |
| `ALIYUN_ACCESS_KEY_ID` | 阿里云访问密钥 ID | ✅ |
| `ALIYUN_ACCESS_KEY_SECRET` | 阿里云访问密钥 | ✅ |
| `AMAP_KEY` | 高德地图 API 密钥 | ✅ |

## 常见问题

### 编译和构建

**Q: Maven 编译失败？**
```bash
# 使用 Maven Wrapper
.\mvnw.cmd clean compile

# 清理并重新构建
.\mvnw.cmd clean install -DskipTests
```

**Q: 找不到 mvnw 命令？**
```bash
# Windows 使用
.\mvnw.cmd

# Linux/Mac 使用
./mvnw
```

### 数据库相关

**Q: 数据库连接失败？**
1. 确认 MySQL 服务已启动
2. 检查 `application-local.yml` 配置
3. 确认数据库 `campuslostandfound` 已创建
4. 测试连接：`mysql -u root -p -e "SHOW DATABASES;"`

**Q: 表不存在错误？**
```bash
# 重新导入表结构
mysql -u root -p campuslostandfound < src/main/resources/schema.sql
```

### Redis 相关

**Q: Redis 连接失败？**
1. 确认 Redis 服务已启动
2. 测试连接：`redis-cli ping`
3. 检查配置文件中的 Redis 地址和密码

**Q: 如何清除 Redis 缓存？**
```bash
redis-cli
> FLUSHDB  # 清除当前数据库
> FLUSHALL # 清除所有数据库
```

### 认证相关

**Q: JWT 令牌验证失败？**
1. 确认请求头包含 `Authorization: Bearer <token>`
2. 检查令牌是否过期（访问令牌 2 小时）
3. 使用刷新令牌获取新的访问令牌

**Q: 登录账户被锁定？**
- 等待 15 分钟自动解锁
- 或手动清除 Redis：`DEL login:fail:{studentId}`

### 文件上传

**Q: 文件上传失败？**
1. 检查文件格式（仅支持 jpg/jpeg/png/gif/webp）
2. 检查文件大小（≤10MB）
3. 确认 OSS 配置正确
4. 检查 OSS 访问权限

**Q: 上传成功但无法访问图片？**
- 检查 Bucket 权限设置（需要公共读或配置签名 URL）
- 确认 OSS 端点配置正确

### 前端相关

**Q: npm install 失败？**
```bash
# 清除缓存
npm cache clean --force

# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

**Q: 前端无法连接后端？**
1. 检查后端服务是否启动（http://localhost:8080）
2. 检查 `web/.env` 中的 API 地址配置
3. 检查浏览器控制台的 CORS 错误

### 开发环境

**Q: Windows 环境配置问题？**
- 查看 [Windows 环境设置指南](docs/WINDOWS_SETUP.md)
- 查看 [环境修复指南](../环境修复指南.md)

**Q: 如何重置开发环境？**
```bash
# 1. 停止所有服务
# 2. 清除数据库
mysql -u root -p -e "DROP DATABASE IF EXISTS campuslostandfound;"

# 3. 重新初始化
cd lostandfound/scripts
init-database.bat

# 4. 清除 Redis
redis-cli FLUSHALL

# 5. 重新启动服务
```

## 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 提交规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat:` 新功能
- `fix:` 修复 Bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 代码重构
- `test:` 测试相关
- `chore:` 构建/工具相关

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 联系方式

- 项目地址：[GitHub Repository]
- 问题反馈：[Issues]
- 文档：[Wiki]

## 致谢

感谢以下开源项目和服务：

- [Spring Boot](https://spring.io/projects/spring-boot)
- [Vue.js](https://vuejs.org/)
- [Element Plus](https://element-plus.org/)
- [MyBatis-Plus](https://baomidou.com/)
- [阿里云](https://www.aliyun.com/)
- [高德地图](https://lbs.amap.com/)

---

<div align="center">

**如果这个项目对你有帮助，请给一个 ⭐️ Star！**

Made with ❤️ by Smart Campus Team

</div>
