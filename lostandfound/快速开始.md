# 🚀 快速开始指南

## 你的配置信息

- **操作系统**: Windows
- **数据库**: MySQL 8.0
- **数据库名**: campuslostandfound
- **用户名**: root
- **密码**: 40619128

## 一键启动（3步完成）

### 第 1 步：测试数据库连接

```cmd
cd lostandfound\scripts
test-connection.bat
```

✅ 如果看到 MySQL 版本信息，说明连接成功！

### 第 2 步：初始化数据库

```cmd
init-database.bat
```

这个脚本会：
- ✅ 创建数据库 `campuslostandfound`
- ✅ 创建 7 张表（user, item, item_image, item_tag, match_record, message, point_record）
- ✅ 询问是否导入测试数据（建议选择 Y）
- ✅ 验证安装

### 第 3 步：启动应用

```cmd
cd ..
mvnw.cmd spring-boot:run
```

等待启动完成后，访问：http://localhost:8080

## 测试账号

如果导入了测试数据，可以使用：

| 学号 | 密码 | 角色 |
|------|------|------|
| 2021001 | password123 | 普通用户 |
| 2021002 | password123 | 普通用户 |
| ADMIN001 | password123 | 管理员 |

## 常用命令

### 启动 MySQL 服务
```cmd
net start MySQL80
```

### 查看数据库
```cmd
mysql -uroot -p40619128 -e "USE campuslostandfound; SHOW TABLES;"
```

### 重新初始化数据库
```cmd
mysql -uroot -p40619128 -e "DROP DATABASE IF EXISTS campuslostandfound;"
cd lostandfound\scripts
init-database.bat
```

### 编译项目
```cmd
cd lostandfound
mvnw.cmd clean compile
```

### 运行测试
```cmd
mvnw.cmd test
```

## 遇到问题？

### MySQL 服务未启动
```cmd
net start MySQL80
```

### 端口 8080 被占用
```cmd
# 查找占用进程
netstat -ano | findstr :8080

# 结束进程（替换 PID）
taskkill /PID [进程ID] /F
```

### 找不到 mysql 命令
将 MySQL 的 bin 目录添加到系统 PATH：
```
C:\Program Files\MySQL\MySQL Server 8.0\bin
```

## 详细文档

- 📖 [Windows 完整设置指南](docs/WINDOWS_SETUP.md)
- 📖 [数据库设计文档](docs/database-schema.md)
- 📖 [数据库设置指南](docs/database-setup-guide.md)
- 📖 [快速参考](docs/QUICK_START_DATABASE.md)

## 下一步

1. ✅ 数据库已配置
2. ✅ 应用已启动
3. 📝 开始开发功能（查看 `.kiro/specs/smart-campus-lost-found/tasks.md`）
4. 🧪 编写测试
5. 📚 查看 API 文档：http://localhost:8080/swagger-ui.html

## 项目结构

```
lostandfound/
├── scripts/
│   ├── init-database.bat      # 数据库初始化脚本
│   └── test-connection.bat    # 连接测试脚本
├── src/
│   ├── main/
│   │   ├── java/              # Java 源代码
│   │   └── resources/
│   │       ├── schema.sql     # 数据库结构
│   │       ├── data-test.sql  # 测试数据
│   │       └── application-local.yml  # 本地配置（已配置）
│   └── test/                  # 测试代码
├── docs/                      # 文档
└── pom.xml                    # Maven 配置
```

## 获取帮助

如果遇到问题：
1. 查看 [Windows 设置指南](docs/WINDOWS_SETUP.md) 的"常见问题"部分
2. 检查 MySQL 服务是否运行
3. 确认配置文件中的密码是否正确
4. 查看应用日志输出

祝开发顺利！🎉
